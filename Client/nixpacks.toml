[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[variables]
NEXT_TELEMETRY_DISABLED = "1"
PUPPETEER_SKIP_DOWNLOAD = "true"
NODE_ENV = "production"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
